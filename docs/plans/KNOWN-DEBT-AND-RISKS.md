# Known Debt and Risks

归档复审中发现的 workaround、一致性遗漏、已知限制与未来增强。按严重度分级。

---

## 🟢 已知限制（设计决策，非 bug）

| Plan | 类别 | 描述 | 参考 |
|---|---|---|---|
| 019 | 已修复 | ~~`load_builtin` 返回 `Option<Box<dyn Role>>`，rust-ref 用 `Option<Arc<dyn Role>>`。转译版 `resolve_role` 因此返回 `Option<Arc<Box<dyn Role>>>`（双层包装）~~ — **已修复（2026-08-06，auto-lang §15.11-followup 跨模块 spec 单层化）**：a2r 现在对跨模块 import 的 spec（`use role_def: Role`）也渲染单层 `Arc<dyn Role>`。`resolve_role` 现返回 `Option<Arc<dyn Role>>`（与 rust-ref 对齐）；`roles.at` 值侧 `Arc(Box(role))` → `Arc(role)`（用户角色路径），`Arc(b)` → `Arc::from(b)`（builtin 路径，Box→Arc 单层转换）。`load_builtin` 仍返回 `Box<dyn Role>`（其声明在 builtin_roles.at，是返回类型本身，非容器包裹——保持 Box 合理）。retranspile + 独立 crate build + 全测试绿。 | `crates/auto-ai-agent/rust/src/roles.rs`（resolve_role 单层）|
| 021 | 已修复 | ~~EventSink actor 的 handler 只能 is-解构单个变体字段，无法把整个 `StreamEvent` 转发给 app~~ — auto-lang Plan 389（R1/R2/R3）已修复：(a) on-block TypeBinding 绑定入 name scope（`forward(ev)` / `(self.cb)(ev)` 可用）；(b) fn 指针可作 task state 字段（`cb: fn(...)` 类型推导）；(c) f-string 自引用 task state 字段可解析。**剩余**：app 注入 cb 到 actor 的机制（spawn 无参 + 无状态写入消息，见 plan 021 §6.7）→ 📌 已立项 auto-lang **Plan 390**（`actor-state-injection`，draft），推荐机制 M1（spawn 带初始化参数）。 | `crates/auto-ai-agent/src/agent.at`（EventSink task） |
| 019 | 已修复 | ~~Phase 2 的 4 类 sed workaround（clone 补全/ReadDir 去引用/path 借用/去多余 as_str）~~ — **已毕业删除（2026-08-20，auto-lang Plan 396 §2.1–§2.4）**：四类 a2r 借用推理根因修毕，agent/retranspile.sh 对应 sed 段验证 no-op 后整段删除（脚本内留有 GRADUATED 注释）。 | `crates/auto-ai-agent/retranspile.sh` Plan 019 Phase 2 段（已删）|
| 021 | 已修复 | ~~a2r 对 `Arc(x)`/`Box(x)` 构造函数在**实参位置**（`fn(Arc(x))` / `self.m(Arc(x))`）渲染为 `Arc(x)` verbatim（应为 `Arc::new(x)`）~~ — auto-lang **Plan 390 §14 L1 根因已修（1317d91c）**：parser `node_or_call_expr` 补 Arc/Box + `(` 识别 → `Expr::ArcExpr` → `Arc::new(x)`，实参位与 let 位一致。**已回收（cfa1515）**：`tool.at` 已去 `let a = Arc(tool)` workaround，直写 `self.tools.set(n, Arc(tool))`。 | `crates/auto-ai-agent/src/tool.at`（register，let-bound Arc workaround）|
| 021 | 已修复 | ~~Auto/a2r **缺少闭包类型**（`Box<dyn Fn>`/`impl Fn`/`Arc<dyn Fn>`）。EventSink cb 是裸 `fn(StreamEvent)` 指针，能持不捕获的 fn（noop_event），但不能持捕获了 `on_event` 的闭包。阻塞 driver Delta/Tool 转发（Plan 021 Phase 6.6）~~ — **类型机制已交付（Plan 390 §15.10）+ auto-ai 侧已落地（Plan 021 Phase 6.6，2026-08-06）**：`spec Fn` + `Box<Fn>` → `Box<dyn Fn + Send + Sync>`（含带签名 `Box<Fn(A)>`），闭包字段默认值自动 `Box::new(move |..|)`。EventSink cb 改 `cb = fn(e StreamEvent) { }` 闭包字段；driver drive_step 用 `Box(move (e) => forward_stream_event(e, on_event_local))` 注入捕获 `on_event` 的闭包，`run_stream` 流式转发 Delta/Tool → PipelineEvent 全部接通。rust-ref 用 `Arc<dyn Fn>`，Auto 现表达 `Box<dyn Fn>`（语义等价：box 而非 arc——单 sink 不需共享所有权）。端到端 smoke PASS（闭包捕获 `Arc<Mutex<Vec>>` 转发 Delta "hello world"）。driver 流式与非流式事件均工作。 | `crates/auto-ai-agent/src/agent.at`（EventSink task cb 字段）、`crates/auto-ai-agent/src/orchestration/driver.at:303`（drive_step run_stream 流式）|
| 021 | 已修复 | ~~a2r **跨模块 import 的 spec 不享 §15.11 单层化**。`Arc<Tool>` 在 `tool.at`（`pub spec Tool`，同模块）渲染单层 `Arc<dyn Tool>`，但在 `agent.at`（`use tool: Tool`，跨模块 import）仍渲染双层 `Arc<Box<dyn Tool>>`~~ — **已修复（2026-08-06，auto-lang §15.11-followup）**：a2r 的 `Box<Spec>`/`Arc<Spec>` 单层化现覆盖**跨模块 import 的 spec**（`use mod: Spec` 的 spec 名在 `spec_decls` 中时，`Type::User` 也走单层 `Arc<dyn T>` 分支，不再仅 `Type::Spec`）。`agent.rs` `register_shared` 现原生渲染 `Arc<dyn Tool>`；retranspile.sh 的 §15.11 sed 已删除（变 no-op 后清理）。同根因的 `Role` 双层也一并解除（见上 019 行）。验证：三转译 crate 独立 build 0 错（无 sed）、workspace 全绿、端到端 smoke（`Arc::new(PingTool)` 单层直注）PASS。 | `crates/auto-ai-agent/retranspile.sh`（§15.11 sed 已删）、`crates/auto-ai-agent/rust/src/agent.rs:288`（register_shared 原生单层）|
| — | 已修复 | ~~**a2r await-drop 回归（2026-08-23，auto afe30bf8 复现）**~~ — **当日修复并验收（auto-lang e29c3b92 / cbb2c0b3，fix-a2r-await-regress）**：`.at` 源码显式 `.await` 在 return/let/is-match 位被丢弃（agent 17 处 + daemon ollama 2 处）。曾以 sed 兜底（agent 8 条 + daemon 2 条），修复后验证 no-op 毕业删除（9727528）。 | `crates/auto-ai-agent/retranspile.sh`（await-drop 段已删）|
| — | 已修复 | ~~**a2r 发射形状漂移致 daemon 旧 sed 锚点失配（2026-08-23）**~~ — **当日修复并验收（同上 e29c3b92）**：clone 形状还原（`m.content`/`t.clone()` 复原）、`.length`→`.len()` 映射恢复、record 字面量→`serde_json::json!` 发射、const 推断复原。曾补的 5 条新锚点 sed 与 anthropic.at 的 is_empty/Map 手工构造规避写法均毕业还原（9727528，`.length` + record 字面量原始写法重生成 0 错）。 | `crates/auto-ai-daemon/retranspile.sh`（2026-08-23 段已删）、`crates/auto-ai-daemon/src/anthropic.at:125` |
| — | 已知限制 | agent/retranspile.sh 现存 2 条独立 sed：① SOUL const 类型修复（comptime `read_text` 产字符串字面量但 a2r 推断 `/* unknown */`，sed 改 `&str`，Plan 016 3.1）；② correct_handoff_target 借用反转（a2r 把 `mut eng` 参数渲染成 `&mut`，sed 改回按值，Plan 387 §16 aftermath）。另：handoff.at 的 `routes`→`entries` 改名规避（`routes` 是 .at 保留字，同 TierRouting 先例）。 | `crates/auto-ai-agent/retranspile.sh`（L159/L178 段）、`crates/auto-ai-agent/src/orchestration/handoff.at:73` |

## 📋 未来增强

| Plan | 类别 | 描述 | 参考 |
|---|---|---|---|
| 019 | 已完成 | ~~a2r 借用推理的 4 类根因（B/C/D/E）应到 auto-lang 根因修复，届时删除 retranspile.sh 的对应 sed 规则~~ — **Plan 396 §2.1–§2.4 已修完（2026-08-20）**，agent 侧四类 sed 验证 no-op 后毕业删除；全量重生成归零（156c6c8）。 | `crates/auto-ai-agent/retranspile.sh`（GRADUATED 注释段）|
| 021 | 已完成 | ~~a2r **跨模块 import 的 spec 单层化**（§15.11 扩展）~~ — **已修复（2026-08-06，auto-lang §15.11-followup）**：a2r 现对 `use mod: Spec` import 的 spec 也单层化。retranspile.sh 的 §15.11 sed 已删除（见上 021 已修复行）。 | — |
| 020 | 已完成 | ~~ai-config/rust + auto-ai-client/rust 转译版错误清零~~ — Plan 020 已通过 retranspile.sh sed 绕过清零（ai-config 1→0、client 8→0）。**残留（2026-08-23 复核）**：client 侧仍有 7 条 sed、ai-config 侧 2 条——其中部分是永久性重写（mut stream、as u32 窄化、as_array 解包、JsonValue 别名注入），并非"根因修复后自动 no-op"性质；Plan 396 根治的是 agent 侧 B/C/D/E 四类，client/ai-config 的同类残留未随批清理。 | `crates/ai-config/retranspile.sh`、`crates/auto-ai-client/retranspile.sh` |
| 022 | 已消费 | ~~**转译版 ReAct 循环非流式**（complete vs complete_stream）~~ — **Plan 022 follow-up 已落地（2026-08-07）**：`agent.at` 的 `spec Client` 加了 `complete_stream(req, on_event Arc<Fn(JsonValue)>)` 方法（无默认体——a2r DIV-TRAIT-A2R-3 不支持 spec 默认体调 self）；`run_inner` 改用 `complete_stream`，回调（`Arc(move (ev) => forward_sse_delta(ev, sink_cb))`）把 SSE text delta 转发为 `StreamEvent.Delta`（per-token 流式复活）；`client_impl.rs` 的 `AiClient`/`StreamingAiClient` impl 加 complete_stream 桥接真实 SSE；过时注释已更新。**保守增量**：统一发 Delta（reasoning→Thinking 区分留后续）。转译版测试 T15 验证 Delta 转发。依赖 auto-lang Plan 397（spec supertrait + Arc<Fn> spec-param）+ TaskRef Clone（auto-lang a6abb487，解除 sink move 重用 E0382）。`StreamingAiClient` 侧信道 mpsc 保留（main.rs printer 仍用），complete_stream 同时喂正式回调 + 侧信道。 | `crates/auto-ai-agent/src/agent.at`（spec Client:111、run_inner:414、forward_sse_delta:607）、`rust/src/client_impl.rs`（complete_stream 桥接）|
| 022 | 已消费 | ~~**转译版 trait API 分叉**：`Tool`/`Role`/`Client`/`AgentFactory` trait 无 `Send+Sync` bound~~ — **Plan 022 follow-up 已落地（2026-08-07）**：4 个 spec 已加 `: Send + Sync`（`tool.at`/`role_def.at`/`agent.at`/`driver.at`），转译输出 `trait X: Send + Sync`（auto-lang Plan 397 能力）。**剩余子项（文档化，非阻塞）**：②&str 返回（codegen 故意选择）、⑤Arc 默认（源码可控写 `Arc<T>`）；③owned（`@T` 已支持）、④u32（`usize`/`u64` 关键字已有）= 非缺口，auto-ai 源码层面可改（转正时）；⑥spec 内泛型方法 + `'static`（大工程，`*_shared` workaround 可用）= 留作限制。详见 `docs/reviews/005-transpiled-parity-matrix.md` §8.2。 | `crates/auto-ai-agent/src/{tool,role_def,agent}.at`、`orchestration/driver.at` spec 声明 |
| 022 | 已修复 | ~~`register_skill_tool` 只存 skills_block 不注册 tool（skill 工具调用 "tool not found"）~~ — **Plan 022 Phase 3.1 已修复（2026-08-07）**：`agent.at` 的 `register_skill_tool` 现在存 block 后调用 `self.register_tool(Box(tool))`（Box 构造触发 unsizing coercion），对齐 rust-ref 既注册又存的行为。空 block 存 None。转译版测试 T9 验证。 | `crates/auto-ai-agent/src/agent.at:302`（register_skill_tool） |
| 022 | 已修复 | ~~`build_request` 硬编码 `preferred_provider: None`（Role 偏好丢失）~~ — **Plan 022 Phase 3.2 已修复（2026-08-07）**：`agent.at` build_request 改 `preferred_provider: self.role.preferred_provider()`（Role trait 方法本已存在，只是未被调用）。转译版测试 T14 验证偏好传播到 CompletionRequest。 | `crates/auto-ai-agent/src/agent.at:517`（build_request） |
| 022 | 已验证 | ~~**Live e2e 实际运行**。转译版 agent 的完整运行链路（转译版 Agent → StreamingAiClient → 真实 AiClient → daemon → LLM）设施已就绪但未实跑~~ — **Plan 025 Phase 5 已验证（2026-08-07）**：全 Auto 版三栈链路跑通（`scripts/e2e-daemon-a2r.sh`）：转译版 agent → 转译版 client → **转译版 daemon** → LLM，agent 输出 "hello world"。转译版 daemon 与原生版 /v1/chat/completions 同 prompt 响应逐字一致（含 token 计数）。 | `scripts/e2e-daemon-a2r.sh`（Plan 025 Phase 5）、`scripts/e2e-transpiled.sh`（Plan 022，原生 daemon）|
| 024 | 已修复 | ~~**转译版 client 的 sync-in-async**。`auto-ai-client-a2r` 的 `complete`/`complete_stream` 声明为 `pub async fn`，但底层用 a2r-std http（ureq，同步）+ 后台线程，async fn 实际阻塞 tokio 执行器~~ — **Plan 024 sync-in-async 修复（2026-08-07）**：`client_impl.rs` 的 trait impl 用 `tokio::task::spawn_blocking` + 独立 current-thread runtime（`Runtime::new().block_on`）包裹转译版 AiClient 的 async 方法，把同步 ureq I/O 隔离到 tokio 阻塞线程池，executor 不再被阻塞。AiClient 仅含 String URL，clone 廉价。e2e 实跑确认功能正常。**附注**：a2r-std http 也加了 async API（`send_async`/`AsyncHTTPStream`），但 a2r 方法链转译不支持 `.send_async()`（builder 链断裂），故 .at 源码仍用同步 http，async 化在胶水层完成。待 a2r 修方法链转译后可直接用 async API（可选优化）。 | `crates/auto-ai-agent/rust/src/client_impl.rs`（run_blocking + spawn_blocking）、`auto-lang/crates/a2r-std/src/http.rs`（async API 已加但未用）|
| 025 | 已知限制 | **`tokio::select!` 无 .at 语法** → openai/anthropic 的 `complete_stream`（cancel 响应 + idle-timeout 竞速循环）整体留 `provider_glue.rs` 手写（openai_complete_stream / anthropic_complete_stream）。a2r-std 的 `post_stream_with_headers_async` 虽可异步流式，但无法表达 select! 的 biased 竞速语义（客户端断连立即停拉 token、上游卡死 30s 超时）。openai.at/anthropic.at 的 complete_stream 方法委托这两个 glue 函数。daemon 的非流式 complete() + build_body 全从 .at 转译。**解除条件**：给 .at 加 select! 语法（auto-lang follow-up，文档 Plan 025 §2.1 选项 B）。 | `crates/auto-ai-daemon/rust/src/provider_glue.rs`（openai/anthropic_complete_stream）、`src/{openai,anthropic}.at`（complete_stream 委托）|
| 025 | 已知限制 | **`impl Drop` 无 .at 语法** → server.rs 的 `CancelOnDrop`（RAII guard，SSE 客户端断连时触发 CancellationToken）留 `server_glue.rs` 手写。streaming_response 也整体留 server_glue（裸 tokio::spawn + 双向 mpsc + async_stream::stream! 三件套无 .at 转译先例，仅 actor 抽象的 spawn + 单向 recv 有）。server.at 的 chat_completions 流式分支委托 server_glue::streaming_response。**解除条件**：a2r 支持 impl Drop + 裸 spawn/mpsc/stream! 转译（或用 actor 抽象重写 SSE 桥接）。 | `crates/auto-ai-daemon/rust/src/server_glue.rs`（CancelOnDrop + streaming_response）、`src/server.at`（chat_completions 流式分支委托）|
| 025 | 已知限制 | **main.rs 手写**（非转译）。a2r 把 `println`/`eprintln` 渲染成函数调用（应为宏 `println!`），`env.args()` 路由到不存在的 `a2r_std::env::args`，且 `#[tokio.main]` 双重输出。bin 是纯胶水（arg 解析 + bind + serve），沿 auto-ai-agent/rust/src/main.rs 的手写 bin 模式。axum 验证里程碑在 server.at（lib）。**解除条件**：a2r 支持 println!/eprintln! 宏 emit + env.args + tokio.main 去重。 | `crates/auto-ai-daemon/rust/src/main.rs`（手写 bin）|
| 025 | 已知限制 | **services.rs 直接复制**（非转译，233 行）。含 `cfg!(windows)`、`std::process::Command`、`reqwest::blocking::Client`、`spawn_blocking` 等 OS 特定胶水，转译风险高且与 axum 核心验证无关。server.at 的 services_list/services_ensure handler 委托它（handler 本身在 server_glue.rs）。**解除条件**：a2r 支持 cfg! + Command 链式（低优先级，纯 OS 胶水）。 | `crates/auto-ai-daemon/rust/src/services.rs`（直接复制自 rust-ref）、`rust/src/server_glue.rs`（services_list/ensure）|
| 025 | 已知限制 | **server 框架 wiring 留 server_glue.rs**。`build_router` 的 ServeDir（frontend-dist 静态资源）+ CorsLayer + `env!("CARGO_MANIFEST_DIR")`（编译期宏）+ route_service 挂载无法在 .at 表达（tower_http 层 + proc-macro）。server.at 的 router() 委托 server_glue::build_router。config_test（reqwest 客户端链式）也留 server_glue。**解除条件**：a2r 支持 tower_http layer + env! 宏（低优先级）。 | `crates/auto-ai-daemon/rust/src/server_glue.rs`（build_router + config_test）、`src/server.at`（router 委托）|
| 025 | 已知限制 | **daemon retranspile.sh 的 156 条 sed workaround**（Plan 025 Phase 1-4 累积）。每条精确锚定模式并注释标注缺陷类别，涵盖 a2r 的多类 codegen 缺陷：(a) 借用推理（HashMap::get 的 &String/Q 推断、循环变量字段未 clone、&Vec 迭代）；(b) 构造器 self 陷阱（ext 方法的 from_config/complete 被 a2r 加 &self，应为关联函数/正确 &req）；(c) json! 宏替代（Value.Number 整数需 Number::from、temperature f64 需 from_f64）；(d) extractor + IntoResponse（有 extractor 参数时 ~IntoResponse 丢 impl 关键字）；(e) 方法链必须单行（多行链在方法体内断链，receiver 重绑 self）；(f) serde_json::Value::as_array vs a2r_std::json::as_array 歧义。a2r 根因修复后 sed 自动变 no-op。属 Plan 013/016/019 既定的 workaround 模式。 | `crates/auto-ai-daemon/retranspile.sh`（Phase 1-4 各文件 sed 段）→ auto-lang Plan 396 滚动聚合 |
